apiVersion: v1
kind: Namespace
metadata:
  name: secureflow
  # CK-07 — missing required labels (app, team). -remediated
  # Pod Security Admission labels also deliberately absent. - Remediated
  labels:
    app: secureflow
    team: devsecops
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted